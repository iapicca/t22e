name: capability
description: Terminal capability probing (DA1, color, sync, keyboard).
version: 0.0.1
publish_to: none
resolution: workspace

environment:
  sdk: ^3.11.6

dependencies:
  protocol:
    path: ../protocol
  ansi:
    path: ../ansi
  parser:
    path: ../parser
  core:
    path: ../core
  freezed_annotation: ^3.1.0

dev_dependencies:
  build_runner: ^2.15.0
  freezed: ^3.2.5
  lints: ^6.0.0
  test: ^1.25.6
