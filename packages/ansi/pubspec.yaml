name: ansi
description: ANSI escape sequence builders for terminal interaction.
version: 0.0.1
publish_to: none
resolution: workspace

environment:
  sdk: ^3.11.6

dependencies:
  protocol:
    path: ../protocol

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
