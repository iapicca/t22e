name: widgets
description: Terminal widget library with TEA architecture (Model/Msg/Cmd).
version: 0.0.1
publish_to: none
resolution: workspace

environment:
  sdk: ^3.12.0

dependencies:
  protocol:
    path: ../protocol
  core:
    path: ../core
  unicode:
    path: ../unicode
  parser:
    path: ../parser
  freezed_annotation: ^3.1.0
  notifier:
    path: ../notifier

dev_dependencies:
  build_runner: ^2.15.0
  freezed: ^3.2.5
  lints: ^6.1.0
  test: ^1.31.1
