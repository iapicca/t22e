/// Horizontal text alignment.
enum TextAlign { left, center, right }

/// Alignment along the main axis of a container.
enum MainAxisAlignment { start, center, end, spaceBetween, spaceAround }

/// Alignment along the cross axis of a container.
enum CrossAxisAlignment { start, center, end, stretch }

/// Orientation axis for containers and scrollables.
enum Axis { horizontal, vertical }

/// Echo mode for text input fields.
enum EchoMode { normal, password, noEcho }

/// Border drawing style for Box widgets.
enum BorderStyle { single, double, rounded, thick }
