name: terminal
description: Terminal raw mode management (FFI + IO).
version: 0.0.1
publish_to: none
resolution: workspace

environment:
  sdk: ^3.12.0

dependencies:
  freezed_annotation: ^3.1.0
  notifier:
    path: ../notifier
  protocol:
    path: ../protocol

dev_dependencies:
  build_runner: ^2.15.0
  ffi: ^2.1.0
  freezed: ^3.2.5
  lints: ^6.1.0
  test: ^1.31.1
