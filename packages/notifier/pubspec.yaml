name: notifier
description: Observable objects with structured disposal lifecycle.
version: 0.0.1
publish_to: none
resolution: workspace

environment:
  sdk: ^3.12.0

dependencies:
  meta: ^1.12.0

dev_dependencies:
  lints: ^6.1.0
  test: ^1.31.1
