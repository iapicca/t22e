name: protocol
description: Terminal protocol byte-level constants.
version: 0.0.1
publish_to: none
resolution: workspace

environment:
  sdk: ^3.12.0

dev_dependencies:
  lints: ^6.1.0
  test: ^1.31.1
