name: testing
description: Widget testing utilities including virtual terminal and widget tester.
version: 0.0.1
publish_to: none
resolution: workspace

environment:
  sdk: ^3.11.6

dependencies:
  protocol:
    path: ../protocol
  core:
    path: ../core
  renderer:
    path: ../renderer
  widgets:
    path: ../widgets
  parser:
    path: ../parser

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
