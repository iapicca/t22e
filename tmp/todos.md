# TODO Map — t22e

This file is generated by scanning the codebase for `TODO` comments. It does
**not** remove or alter any `TODO` marker in source (see
`.ai/ai-guidelines.md`).

Priority: `High` / `Medium` / `Low`
Difficulty: `Easy` / `Medium` / `Hard`

---

## 1. Rethink `stdin` value notifier naming and folder

- **Target file**: `lib/src/async_value/stdin_value_notifier_provider.dart:11`
- **Priority**: Medium
- **Difficulty**: Easy
- **Description**: `// TODO change naming for this! including the folder`. The
  provider lives under `lib/src/async_value/` but the project no longer ships a
  local `AsyncValue` clone (see `.ai/project.md` §6). The folder name and the
  `stdinValueNotifierProvider` symbol are misleading.
- **Proposed fix**: Move the file under `lib/src/io/` (it only wires
  `stdinStreamProvider` to a `ValueNotifier<List<int>>`). Rename the provider to
  `stdinBytesProvider` (or `lastStdinBytesProvider`) and update the barrel +
  callers in `test/providers_test.dart`.


## 6. Revisit the diff-engine approach

- **Target file**: `lib/src/engine/diff_engine.dart:9`
- **Priority**: Medium
- **Difficulty**: Hard
- **Description**: `// TODO I don't like this one bit! looks like TEA
  approach!` — the current cell-by-cell scan emits `{Move, Style, Write}` ops
  and re-derives a canonical style `Cell` per changed cell. Flagged in
  `.ai/project.md` §8 as an open structural TODO.
- **Proposed fix**: Investigate alternative diff strategies (dirty-region
  tracking, run-length encoded style spans, Myers diff over rows). Prototype a
  candidate on a branch and benchmark output size and latency against the
  current implementation before replacing it.

## 7. Rework `StdinReader` by hand

- **Target file**: `lib/src/io/stdin_reader.dart:9`
- **Priority**: Medium
- **Difficulty**: Medium
- **Description**: `// TODO I want to rework this by hand!` — the class is a
  thin `StreamController.broadcast` wrapper over `stdin`. The author wants a
  hand-rolled reader rather than the broadcast-adapter pattern.
- **Proposed fix**: Replace the broadcast adapter with a dedicated
  `StdinReader` that subscribes a single listener-set via a custom
  `StreamController` (or a manual listener list), expose `dispose()` aligned
  with the `Disposable` mixin, and inject the byte source through a Riverpod
  provider for testability.

## 10. Inject the parser's `StreamController` via Riverpod

- **Target file**: `lib/src/io/ansi_parser.dart:91`
- **Priority**: High
- **Difficulty**: Medium
- **Description**: `// TODO top-priority: WHAT THE F***! this should be
  somewhere else! and be initialized and disposed via riverpod!` — `AnsiParser`
  constructs its own `StreamController<InputEvent>.broadcast(sync: true)` in
  the constructor, mixing ownership/lifecycle with parsing logic.
- **Proposed fix**: Split out an `InputEventSink` (or reuse
  `inputEventStreamProvider`) created and disposed in
  `ansiParserProvider`. `AnsiParser` should accept the controller as a
  constructor parameter (`AnsiParser({required StreamController<InputEvent>
  controller})`) so the provider owns lifecycle; `ref.onDispose` closes the
  controller instead of `parser.close()`.

## 11. Convert parser state to a `ValueNotifier`

- **Target file**: `lib/src/io/ansi_parser.dart:98`
- **Priority**: Low
- **Difficulty**: Medium
- **Description**: `// TODO this should be a value notifier` — the
  `_ParserState _state` field is mutated directly during parsing and is not
  observable.
- **Proposed fix**: Wrap `_state` in a `ValueNotifier<AnsiParserState>`
  (using the local `ValueNotifier` clone under `lib/src/notifier/`) so test
  harnesses and tooling can observe parser state transitions. Update
  `_process` to read/write through the notifier.

## 13. Extract ground/escape/csi/flush/emitControl as extensions

- **Target file**: `lib/src/io/ansi_parser.dart:134, 167, 200, 240, 271`
- **Priority**: Low
- **Difficulty**: Medium
- **Description**: `// TODO this can be an extension!` appears on
  `_processGround`, `_processEscape`, `_processCsi`, `_flush`, and
  `_emitControl`. These are state-machine handlers that the author feels belong
  on an extension rather than the core class.
- **Proposed fix**: Introduce `lib/src/io/ansi_parser_extensions.dart` with an
  `@internal extension AnsiParserProcessing on AnsiParser` hosting the five
  methods. The extension needs access to `_buffer`, `_state`, and
  `_controller`; either widen those to `@internal` (library-private to
  `lib/src/io/`) or keep them and expose `@internal` getters used only by the
  extension.

## 14. Move control/SS3/CSI key mapping into a `Key` factory

- **Target file**: `lib/src/io/ansi_parser.dart:282, 297, 302`
- **Priority**: High
- **Difficulty**: Medium
- **Description**: `_controlKey`, `_ss3Key`, and `_csiKey` are static helpers
  in `AnsiParser` that map bytes to a nullable `Key`. The TODO insists they
  have nothing to do with the parser, should be a factory on `Key` returning a
  non-null `Key` (with `_ => Key.unknown`), and that byte constants live in a
  `final class` as `static const int` fields.
- **Proposed fix**: Create `lib/src/io/key/key_factory.dart` exporting
  `Key.fromControl(int byte)`, `Key.fromSs3(int byte)`, and
  `Key.fromCsi(List<int> params, int finalByte)` — each returning `Key`
  (never null) with `Key.unknown` as fallback. Move the relevant byte
  constants to a `final class TerminalBytes { static const int esc = ...; }`
  (relates to tasks #17–#19). Remove the statics from `AnsiParser`.

## 25. Re-evaluate `StdoutInterface` folder structure

- **Target file**: `lib/src/engine/stdout_interface.dart:13`
- **Priority**: Medium
- **Difficulty**: Easy
- **Description**: `// TODO: Re-evaluate folder structure for this class.`
  Storage of the `StdoutInterface` mixin and its default `StdoutWriter`
  implementation under `lib/src/engine/` blurs the engine/io boundary.
  Flagged in `.ai/project.md` §8.
- **Proposed fix**: Pick option (a) from the inline TODO: move the
  implementation to `lib/src/io/stdout_writer.dart` and keep only the
  `StdoutInterface` mixin under `engine/`. Update the barrel and provider
  exports accordingly.

## 27. Revisit `CellBufferBuilder` design

- **Target file**: `lib/src/engine/cell_buffer_builder.dart:8`
- **Priority**: Medium
- **Difficulty**: Medium
- **Description**: `// TODO this looks like bullshit!` — the builder is a thin
  mutable `List<Cell>` accumulator whose `build()` simply wraps the list into
  a `CellBuffer` (no defensive copy). The author questions its value.
- **Proposed fix**: Either inline `CellBuffer` mutation through a
  `mutable`/`growable` view on `CellBuffer` itself (removing the builder), or
  make `build()` copy the cell list (`List.of(_cells)`) so the produced
  `CellBuffer` is guaranteed immutable and cannot be mutated retroactively.
  Add a test asserting the post-build buffer is detached from later writes.

## 28. Implement `Widget` equality / `Key` support

- **Target file**: `lib/src/view/widget.dart:8`
- **Priority**: Medium
- **Difficulty**: Hard
- **Description**: `// TODO: Equality/Key support deferred; identity equality
  is a stopgap.` Widgets use default `Object` identity, preventing
  `Widget.canUpdate`-style element reuse. Listed in `.ai/project.md` §8 under
  deferred widget-layer work.
- **Proposed fix**: Introduce a `Key` type (matching the public API list in
  §5.1), add an optional `Key? key` to the `Widget` constructor, and define
  `Widget.canUpdate(Widget other)` based on `runtimeType` + key. Follow up
  with `StatefulWidget`/`StatefulElement` dirty-flag work called out in §8.

## 30. Re-evaluate `@internal` marker on `cellBufferBuilder` provider

- **Target file**: `lib/src/engine/cell_buffer_builder_provider.dart:12`
- **Priority**: Low
- **Difficulty**: Easy
- **Description**: `/// TODO this is exporting bullshit and shouldn't be
  internal`. The provider takes `width`/`height` family arguments and is
  marked `@internal`; the author flags the export as questionable.
- **Proposed fix**: Decide whether a parameterized builder provider belongs in
  the public API. Probably remove the provider entirely (builders are
  constructed locally by the pipeline) and have callers `new` up the builder
  directly via `terminalSizeProvider`. Alternatively, keep it `@internal` as an
  override-seam and document the rationale.


## 32. Define `RenderText` behavior when constraints force width to 0

- **Target file**: `lib/src/engine/render_text.dart:44`
- **Priority**: Medium
- **Difficulty**: Medium
- **Description**: `// TODO: define the correct behavior when constraints
  force width to 0.` `performLayout` clamps via
  `constraints.constrain(Size(contentWidth, contentHeight))` but does not
  specify what happens (wrapped? clipped? empty?) when `maxWidth == 0`.
- **Proposed fix**: Add an explicit branch in `performLayout`: when
  `constraints.maxWidth == 0`, set `size = Size(0, max(0, contentHeight))` and
  have `paint` early-return. Document the chosen contract on `RenderText` and
  add a regression test asserting zero-width constraints paint nothing and
  report a zero-width size.

## 33. Remove all `ref.watch` usages (banned primitive)

- **Target files**: `.ai/project.md` §2.4; `lib/src/view/components/consumer.dart`;
  `test/view/components/consumer_test.dart`
- **Priority**: High
- **Difficulty**: Easy
- **Description**: `.ai/coding-standards.md` now bans `ref.watch` (it is a
  Flutter reactivity primitive; t22e is Dart-only and uses
  non-autoDispose providers only). Three places still reference it:
  - `.ai/project.md` §2.4 states "ViewModels are plain Riverpod Notifiers
    that `ref.watch` the input stream" — inaccurate and contradicts the
    coding standard.
  - `consumer.dart` exposes a `WidgetRef.watch` path (mirroring
    `flutter_riverpod`).
  - `consumer_test.dart` has a test titled "ref.watch obtains the same
    value as ref.read".
- **Proposed fix**:
  - `.ai/project.md` §2.4: rewrite the line so ViewModels use
    `ref.listen` (or `ref.read` for one-shot) — never `ref.watch`.
  - `consumer.dart`: drop the `WidgetRef.watch` implementation; have
    `Consumer`/`WidgetRef` expose only `read` and `listen` (mirroring the
    project-standard lifecycle). See `.ai/coding-standards.md`
    "Riverpod Provider Lifecycle (pure-Dart)" and the rework notes in
    `tmp/riverpod-rework.md` for why `ref.watch` is wrong here and why
    `ref.read` is *not* ephemeral (the host's `ProviderContainer.dispose`
    is the lifecycle safeguard, since providers are non-autoDispose).
  - `consumer_test.dart`: replace the `ref.watch` test with one that
    asserts the value obtained via `ref.read` matches `container.read`
    for the same provider.
  - After edits: `dart analyze` clean, `dart test` green.